json.extract! shop, :id, :name, :station, :prefucture, :city, :address, :url, :tell_num, :shop_image, :business_hours, :holidays, :features, :created_at, :updated_at
json.url shop_url(shop, format: :json)
